# supervised learning
regression problem
classification model
a training set 数据集,训练数据集
x input feature
y output variable
m number of training example
(x,y) single training example
i th example
algorithm 算法
function(y-hat prediction for y)
substrict
best fine line
(univariate)linear regression 

# unsupervised learning

clustering 聚类
anomaly detection 异常监测
dimensionality reduction 降维

# cost function

f(x)=wx+b
w,b:parameters
w:coefficient
b:weight
squared error cost function=J(w,b)=error+(y hat -y)平方求和m个除以2m
minimize  J(w,b)
slope 斜率
 设定一个w作为f(x)函数，根据当前w算出代价函数的方程，从而得出最适合的w当此时J(w)=0
## b 不设为0
三维碗形
相同J得到F组成椭圆
